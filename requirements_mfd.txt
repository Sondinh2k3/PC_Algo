# Requirements for MFD Analysis Tool
# Core dependencies
pandas>=1.5.0
numpy>=1.21.0
matplotlib>=3.5.0
pyyaml>=6.0

# Scientific computing for smoothing and curve fitting
scipy>=1.9.0

# SUMO dependencies (if not already installed)
# traci is part of SUMO installation
# sumolib is part of SUMO installation

# Optional: for better plotting
seaborn>=0.11.0
